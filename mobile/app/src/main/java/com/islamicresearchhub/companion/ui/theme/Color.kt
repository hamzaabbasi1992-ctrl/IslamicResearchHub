package com.islamicresearchhub.companion.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * The app's one real palette - previously hardcoded independently inside
 * CatalogListScreen only, leaving every other screen on Compose's bare
 * default (lavender) Material3 theme. Named here once so every screen
 * shares the same look via MaterialTheme.colorScheme, not copy-pasted hex.
 */
val EmeraldDarkBackground = Color(0xFF0F172A)
val EmeraldDarkSurface = Color(0xFF1E293B)
val EmeraldDarkSurfaceVariant = Color(0xFF334155)
val EmeraldTeal = Color(0xFF0DAE9B)
val EmeraldTealContainer = Color(0xFF134E4A)
val EmeraldGold = Color(0xFFD4A64A)
val EmeraldSkyBlue = Color(0xFF7DA6E0)
val EmeraldLightText = Color(0xFFF8FAFC)
val EmeraldSubText = Color(0xFF94A3B8)
val EmeraldError = Color(0xFFEF4444)
